apiVersion: v1
kind: Service
metadata:
  name: maybe
  namespace: maybe
  labels:
    app.kubernetes.io/name: maybe
spec:
  selector:
    app.kubernetes.io/name: maybe
  ports:
    - port: 3000
      targetPort: 3000
