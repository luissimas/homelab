apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: readarr-config-local
  namespace: media
  labels:
    app: readarr
  annotations:
    volumeType: local
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
